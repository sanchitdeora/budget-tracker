package models

import (
	// "context"
	"testing"

	"github.com/stretchr/testify/assert"
	// "time"
	// "github.com/golang/mock/gomock"
	// "github.com/sanchitdeora/budget-tracker/models"
	// "github.com/sanchitdeora/budget-tracker/pkg/transaction/mocks"
)

// func createTestBillDb()

func TestGetAllBillRecords(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestGetBillRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestInsertBillRecord(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestUpdateBillRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestUpdateBillRecordIsPaid(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestUpdateBillRecordIsUnpaid(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestDeleteBillRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}