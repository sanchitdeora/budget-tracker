package db

import (
	// "context"
	"testing"

	"github.com/stretchr/testify/assert"
	// "time"
	// "github.com/golang/mock/gomock"
	// "github.com/sanchitdeora/budget-tracker/models"
	// "github.com/sanchitdeora/budget-tracker/pkg/transaction/mocks"
)

// func createTestBudgetDb()

func TestGetAllBudgets(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestGetBudgetRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestInsertBudgetRecord(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestUpdateBudgetRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestDeleteBudgetRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}