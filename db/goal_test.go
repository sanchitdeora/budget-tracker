package db

import (
	// "context"
	"testing"

	"github.com/stretchr/testify/assert"
	// "time"
	// "github.com/golang/mock/gomock"
	// "github.com/sanchitdeora/budget-tracker/models"
	// "github.com/sanchitdeora/budget-tracker/pkg/transaction/mocks"
)

// func createTestGoalDb()

func TestGetAllGoals(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestGetGoalRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestInsertGoalRecord(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestUpdateGoalById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestUpdateGoalRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}

func TestDeleteGoalRecordById(t *testing.T) {
	assert.Equal(t, 1, 1, "The two words should be the same.")
}